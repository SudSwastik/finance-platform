# Copy to infra/local/.env (gitignored) to run docker-compose.cognito.yml against
# the real AWS Cognito pool instead of X-Dev-User-Sub.
COGNITO_REGION=us-east-1
COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
COGNITO_ISSUER_URI=https://cognito-idp.us-east-1.amazonaws.com/us-east-1_XXXXXXXXX
